---
import { Code } from 'astro:components';

const { code, lang, isInline, isWrapped } = Astro.props;
---

<Code
  code={code}
  lang={lang}
  {...isInline ? { inline: true } : {}}
  {...isWrapped ? { wrap: true } : {}}
/>
