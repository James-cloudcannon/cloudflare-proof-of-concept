---
import { Prism } from '@astrojs/prism';


const {
    code,
    lang,
} = Astro.props;


---

<figure class={`hljs code-block language-${lang}`}>
    <Prism
        code={`${code}`}
        lang={lang}
    />

</figure>

